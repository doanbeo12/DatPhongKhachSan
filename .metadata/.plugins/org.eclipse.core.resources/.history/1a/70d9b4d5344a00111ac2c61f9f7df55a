package hotel;

import javax.swing.*;
import java.awt.*;
import javax.swing.border.LineBorder;

public class LoginFrame extends JFrame {

    private JTextField txtUsername;
    private JPasswordField txtPassword;
    private JButton btnLogin;
    private TaiKhoanService tkService;

    public LoginFrame() {
        tkService = new TaiKhoanService();

        setTitle("Đăng nhập");
        setSize(500, 700); // Kích thước dọc theo mẫu hình ảnh
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // Panel chính với màu nền đen chủ đạo
        JPanel panel = new JPanel();
        panel.setBackground(new Color(18, 18, 18));
        panel.setLayout(new GridBagLayout());
        GridBagConstraints gbc = new GridBagConstraints();
        gbc.insets = new Insets(15, 10, 15, 10); // Khoảng cách giữa các thành phần
        gbc.fill = GridBagConstraints.HORIZONTAL;

        // 1. Logo phía trên cùng (Hình oval)
        JLabel lblLogo = new JLabel("*LOGO KHÁCH SẠN Ở ĐÂY", SwingConstants.CENTER);
        lblLogo.setForeground(Color.WHITE);
        lblLogo.setPreferredSize(new Dimension(350, 120));
        lblLogo.setBorder(new LineBorder(Color.WHITE, 1, true));
        gbc.gridx = 0;
        gbc.gridy = 0;
        gbc.gridwidth = 2;
        panel.add(lblLogo, gbc);

        // 2. Nút Đăng nhập lớn (Tiêu đề trạng thái)
        btnLogin = new JButton("Đăng nhập");
        btnLogin.setFont(new Font("Arial", Font.PLAIN, 24));
        btnLogin.setForeground(Color.WHITE);
        btnLogin.setBackground(new Color(18, 18, 18));
        btnLogin.setFocusPainted(false);
        btnLogin.setBorder(new LineBorder(Color.WHITE, 1));
        btnLogin.setPreferredSize(new Dimension(300, 60));
        gbc.gridy = 1;
        panel.add(btnLogin, gbc);

        // 3. Nhãn và Ô nhập Tên đăng nhập
        JLabel lblUser = new JLabel("Tên đăng nhập");
        lblUser.setForeground(Color.WHITE);
        lblUser.setFont(new Font("Arial", Font.BOLD, 14));
        gbc.gridy = 2;
        gbc.gridwidth = 1;
        panel.add(lblUser, gbc);

        txtUsername = new JTextField();
        txtUsername.setBackground(Color.BLACK);
        txtUsername.setForeground(Color.WHITE);
        txtUsername.setCaretColor(Color.WHITE); // Con trỏ chuột màu trắng
        txtUsername.setBorder(new LineBorder(Color.GRAY, 1));
        txtUsername.setPreferredSize(new Dimension(250, 35));
        gbc.gridx = 1;
        panel.add(txtUsername, gbc);

        // 4. Nhãn và Ô nhập Mật khẩu
        JLabel lblPass = new JLabel("Mật khẩu");
        lblPass.setForeground(Color.WHITE);
        lblPass.setFont(new Font("Arial", Font.BOLD, 14));
        gbc.gridx = 0;
        gbc.gridy = 3;
        panel.add(lblPass, gbc);

        txtPassword = new JPasswordField();
        txtPassword.setBackground(Color.BLACK);
        txtPassword.setForeground(Color.WHITE);
        txtPassword.setCaretColor(Color.WHITE);
        txtPassword.setBorder(new LineBorder(Color.GRAY, 1));
        txtPassword.setPreferredSize(new Dimension(250, 35));
        gbc.gridx = 1;
        panel.add(txtPassword, gbc);

        // 5. Nút Đăng ký (Chuyển sang màn hình đăng ký)
        JButton btnGoToRegister = new JButton("Đăng ký");
        btnGoToRegister.setContentAreaFilled(false);
        btnGoToRegister.setForeground(Color.WHITE);
        btnGoToRegister.setFocusPainted(false);
        btnGoToRegister.setBorder(BorderFactory.createMatteBorder(0, 0, 1, 0, Color.WHITE)); // Gạch chân
        gbc.gridx = 0;
        gbc.gridy = 4;
        gbc.gridwidth = 1;
        panel.add(btnGoToRegister, gbc);

        // 6. Nút Quên mật khẩu
        JButton btnForgot = new JButton("Quên mật khẩu?");
        btnForgot.setContentAreaFilled(false);
        btnForgot.setForeground(Color.WHITE);
        btnForgot.setFocusPainted(false);
        btnForgot.setBorder(BorderFactory.createMatteBorder(0, 0, 1, 0, Color.WHITE));
        gbc.gridx = 1;
        panel.add(btnForgot, gbc);

        // Footer giả lập thông tin liên hệ như trong hình
        gbc.gridy = 5;
        gbc.gridx = 0;
        gbc.gridwidth = 2;
        gbc.insets = new Insets(50, 0, 0, 0);
        JSeparator line = new JSeparator();
        panel.add(line, gbc);

        add(panel);

        // Xử lý sự kiện đăng nhập
        btnLogin.addActionListener(e -> {
            String username = txtUsername.getText();
            String password = new String(txtPassword.getPassword());
            boolean success = tkService.dangNhap(username, password);

            if (success) {
                JOptionPane.showMessageDialog(this, "Đăng nhập thành công!");
                // Mở màn hình chính ở đây
            } else {
                JOptionPane.showMessageDialog(this, "Sai tên đăng nhập hoặc mật khẩu");
            }
        });

        // Qua màn hình đăng ký
        btnGoToRegister.addActionListener(e -> {
            new RegisterFrame().setVisible(true);
            dispose();
        });
    }
}